# Roadmap — seo-optimizer

## Status
Current phase: Phase 4 — Orchestration & Scaling
Last updated: 2026-09-18

## Phase 0 — Foundation
- [x] Create monorepo structure with TypeScript workspace packages
- [x] Implement @seo/core types (Check, CheckState, Readiness)
- [x] Build @seo/crawler with fetch, extract, robots.txt parser, URL normalizer
- [x] Load and validate versioned corpus (@seo/corpus from YAML)
- [x] Create @seo/testkit with in-memory fixture website server
- [x] Configure TypeScript project references and vitest testing framework
- [x] Compile v4.4 corpus YAML from TSV source (phases 0-7, 97 checks)

## Phase 1 — Probes & Persistence
- [x] Implement @seo/probes detector registry (6 categories: delivery, indexability, markup, media, metadata, site)
- [x] Build probe matrix tool (npm run probes:matrix) to verify detector coverage
- [x] Design Postgres schema with Drizzle (sites, audits, crawls, pages, links, probes, renders, checks)
- [x] Implement @seo/persistence crawl sink for streaming pages to database
- [x] Map crawl/probe results to database rows with integrity constraints
- [x] Create initial Drizzle migrations (0000-0003) with enum types and indexes
- [x] Write integration tests for persistence and probe runs against live Postgres

## Phase 2 — CI/CD & Deployment
- [x] Write README with setup, build, test, and database management instructions
- [x] Add GitHub Actions CI workflow with Postgres service and full test suite
- [x] Implement .githooks/pre-push for local typecheck and test gate on master
- [x] Create docker-compose.yml with Postgres and Redis for local development
- [x] Configure .env.example with DATABASE_URL and REDIS_URL defaults
- [x] Enable corpus compilation and matrix tools as npm run scripts
- [x] Harden the master ruleset: require pull requests and both the test and roadmap status checks

## Phase 3 — Pre-Release Validation
- [x] Audit triage sign-off: confirm automation tier and remediation class for all 97 checks (scripts/triage.ts requires sign-off before release)
- [x] Settle the corpus source of record: the TSV bootstraps a version, the YAML owns it thereafter, and `corpus:compile` refuses to overwrite an existing version
- [x] Split corpus tests into frozen provenance (v4.4 against its workbook) and structural invariants that run against every version on disk
- [x] Validate site-profile flags against the pinned corpus before an audit runs, so a typo cannot silently excuse launch gates
- [x] Prove two corpus versions load and grade side by side (packages/corpus/test/versions.test.ts, fixtures v9.0/v9.1)
- [x] Amend the v4.4 triage: 3.11 moves from `automated` to `assisted`, signed off by the maintainer 2026-09-11 (corpus/v4.4/phase-3.yaml, scripts/triage.ts)
- [x] Amend the v4.4 triage: 3.9 moves from `automated` to `assisted`, signed off by the maintainer 2026-09-11 (corpus/v4.4/phase-3.yaml, scripts/triage.ts)
- [x] Triage sign-off for corpus v5.0, signed off by the maintainer 2026-09-14 (v4.5 was skipped: the engine went from v4.4 straight to v5.0). Drafted and signed off 2026-09-14 in `scripts/triage.ts`: 15 rows move `automated` → `assisted` (1.3, 1.4, 1.5, 1.6, 1.7, 2.7, 2.9, 5.1, 5.2, 5.4, 6.1, 6.2, 6.5, 7.3, 7.9), the 3.9 content detectors move to 3.5, review integrity moves from 2.11 to 3.13, and 2.18 is new
- [x] Compile corpus v5.0 from the verified workbook (SEO-Launch-Checklist-v5.0.xlsx, SHA-256 1165d18b…612a): 98 checks, 54 launch gates, 108 sources cited by stable id, a frozen `provenance-v5.0.test.ts`, and `CURRENT_CORPUS_VERSION` in @seo/corpus so the analyzer and the probe matrix follow the adopted methodology
- [x] Tie a site profile to the corpus version it was declared against, so a version that makes a universal check conditional cannot excuse it on a flag nobody was asked about (@seo/db: `sites.profileCorpusVersion`, migration 0007 recording `4.4` on existing filled-in profiles; @seo/scheduler: `StaleSiteProfileError`, permanent, raised before the crawl)

## Phase 4 — Orchestration & Scaling
- [x] Implement job queue for managing concurrent crawls (@seo/queue: bounded concurrency, lane exclusion per origin, cancellation)
- [x] Add audit scheduler to trigger crawls on demand or via API (@seo/scheduler: submit returns an audit id before the crawl runs; one audit at a time per origin)
- [x] Build retry logic and error recovery for failed audits (@seo/queue: held jobs, backoff, retry-aware cancellation; @seo/scheduler: which failures repeat, and the audit row across attempts)
- [x] Handle multiple concurrent site audits without resource contention (@seo/scheduler: `auditLane` lanes an audit on the host its requests reach, not the origin string; @seo/queue: a leasing queue asks `JobStore.acquire` for a laned job's lane before running it and waits a beat on `lane-held`; @seo/job-store: `acquire` under an advisory lock on `(queue, lane)`, and `load` writes recovered jobs back as `queued`)
- [x] Claim abandoned jobs while running, not only in `recover()` (@seo/queue: `JobStore.adopt`, asked on every heartbeat, and a heartbeat that keeps watching on an unref’d timer when the queue holds nothing of its own; @seo/job-store: the adoption query, capped at `ADOPTION_BATCH` and never this owner’s own rows; @seo/scheduler: an `adopted` event puts the audit row back to `pending`)
- [x] Recompute the lane of a job recovered from a store written before `auditLane` — it keeps the origin string it was stored with, so for one deploy's backlog an old-format and a new-format lane for one host can run side by side (@seo/queue: `JobQueueOptions.reviveLane`, called by `#restore` — shared by `recover()` and adoption — instead of trusting `StoredJob.lane`; @seo/scheduler: wired to `(job) => auditLane(job.origin)` whenever a store is attached)
- [x] Back the job queue with durable storage so a restart does not lose queued audits (@seo/queue: a `JobStore` seam; @seo/job-store: the `jobs` table behind it; @seo/scheduler: `recover()` on the way up)
- [x] Expire and renew job leases so a second worker can share one queue namespace (@seo/job-store: `leaseMs`, a claim that ages out by the database clock, `renew`, and writes refused on a row that has moved on; @seo/queue: `heartbeatMs` and `JobLeaseLostError`; @seo/scheduler: `heartbeatMs`, and a `reconcile` that leaves another worker's audits alone)
- [x] Reconcile audits left `pending` with no job behind them (@seo/scheduler: `reconcile()` closes out rows nothing is going to run, bounded by the database clock at recovery)
- [x] Give crawl() cooperative cancellation so a cancelled job stops mid-crawl rather than at the end (@seo/crawler: a `signal` checked between requests and inside the politeness delay; a cancelled crawl reads `cancelled`, not `failed`)
- [x] Grade probe evidence into checkStates and freeze readiness on the audit (@seo/grader: verdicts, evidence trail, frozen readiness)
- [x] Make the queue tests deterministic about time (@seo/queue tests: a cancelled job waiting out its backoff is proved gone from the queue rather than observed not to have run within a sleep)
- [x] Implement more of the corpus's detectors — 69 of v5.0's 134 today (was 48 of v4.4's 128), covering 18 of 26 automated checks and 23 launch gates under v5.0 (23 of 41 under v4.4). Closed 2026-09-18 as a batch: what remains is one checkbox per detector below, so the roadmap runner gets a task a single session can finish. `domain-expiry-rdap` (1.18, a launch gate) is in, reading the registry's own RDAP record through rdap.org for the expiration event, registrar and status: a lapsed or deleting domain fails, one inside 30 days warns, and a lookup that answers nothing is `error`, never a finding. 1.7, 1.12, 1.15, 2.7, 2.14, 2.18, 3.9 and 3.11 are complete: `news-sitemap` and `news-article-policy`, `answer-first-structure`, `author-date-signals`, `content-accessibility`, `http-version`, `parameter-crawl-space` and `faceted-nav-control` joined `video-watch-page`, `videoobject-schema`, `video-sitemap`, `product-variant-canonical`, `product-lifecycle-state` and `schema-eligibility-matrix`. `private-response-caching` is also in. 4.1's two raw-crawl detectors, `broken-links` and `metadata-completeness`, are in; 4.1 stays ungraded until Phase 5 supplies `raw-rendered-crawl-diff`. `trust-pages-presence` (3.4, a launch gate) is in. `product-schema` (2.11, a launch gate) is in, judging a product page's own Product markup; 2.11 stays ungraded end to end until `merchant-feed-parity` exists to compare it against a Merchant Center feed. `locale-content-parity` (3.12, a launch gate) is in, catching a locale pair whose headings are word-for-word identical. `outbound-link-qualification` (3.13, a launch gate) is in, failing an outbound link with no sponsored/ugc/nofollow relationship on a page the site itself declares advertising; 3.13 stays ungraded end to end until `ugc-governance` and `review-integrity` exist. `paywall-access-model` (2.15, a launch gate) is in, judging only the markup a gated page can ship on its own — `isAccessibleForFree` and, where it names a partial preview, `cssSelector` on each `hasPart` section; the reader/crawler access-parity half of 2.15 stays a person's, since it needs requests under more than one identity that this engine does not make. `batch-page-quality` (v5.0 3.9, a launch gate — a different question from v4.4's 3.9, now 3.5's `answer-first-structure`/`author-date-signals`) is in: a `bulk-publishing` site whose pages divide into an identical sequence of headings and word counts under different URLs, three or more of them, is template-only output on its face; entities, unsupported promises, factual errors and doorway patterns that do vary their word counts stay with 3.9's risk-based sample, since bodies never reach this engine to compare word-for-word. `image-discoverability` (2.3) is in, failing a page's og:image/twitter:image only when robots.txt keeps Googlebot-Image from a same-site copy of it; Google's own size/aspect recommendations stay a person's record, per the check's own wording. `indexability-matrix-reconciliation` (4.2, a launch gate) is in, failing a same-site stylesheet or script robots.txt keeps Googlebot from fetching; the check's wider matrix of cache, auth and directive reconciliation against an approved plan (0.3) is not something a crawl holds an opinion about, so 4.2 stays ungraded end to end. The remainder need evidence this engine does not yet gather: Search Console, Lighthouse/CrUX, a rendered DOM (Phase 5), or a previous audit to compare against
- [x] Implement detector `cannibalization` (3.10, @seo/probes `content.ts`): fail two or more indexable, self-canonical pages on distinct URLs whose `<title>` or h1 are identical after trimming and case-folding; a shared title on pages that canonicalize to one of them is consolidation, not cannibalization, and is left alone. The check is `assisted`, so a pass is a proposal
- [x] Implement detector `launch-content-completeness` (3.7, a launch gate, @seo/probes `content.ts`): fail an indexable page whose reading matter carries placeholder text on its face (`lorem ipsum`, `TODO`, `TBD`, `coming soon`, `[insert`, `xxx`) or has no reading matter at all; warn on a page with under 50 words of it. Which URLs are launch-critical is the inventory's (0.3) to say, so scope stays the whole crawl
- [x] Implement detector `analytics-implementation` (2.5, a launch gate, @seo/probes `markup.ts`): from the HTML alone, record the GA4 measurement ids and GTM container ids each page loads (`gtag/js?id=`, `gtm.js?id=`, `G-`/`GT-`/`GTM-` literals in inline script); fail a site where one page loads the same id twice or pages disagree on which id they load; warn on a site with no tag anywhere. Event payloads and consent states are the person's half
- [x] Implement detector `consent-mode-config` (2.6, a launch gate, @seo/probes `markup.ts`): where a page loads a Google tag, fail one whose inline script sets a `gtag('consent','default',…)` after the tag is loaded, or sets none while a consent banner is declared (a `<script>` from a known CMP host); pass a default that precedes the tag. Behaviour under each consent state stays a rendered-DOM question
- [x] Implement detector `publisher-discover-readiness` (2.16, @seo/probes `media.ts`): on pages typed as articles, fail an `og:image` whose declared width is under 1200 px or a `max-image-preview` robots directive other than `large`; warn on an article with no `og:image`; not-applicable on a site with no article pages. Indexed evidence and Preferred Sources stay the person's record
- [ ] Implement detector `review-integrity` (3.13, a launch gate, @seo/probes `markup.ts`): fail a `Review` or `AggregateRating` node whose `itemReviewed` is the publishing `Organization` itself (self-serving), a `Review` with no `author`, and an `AggregateRating` with `ratingCount`/`reviewCount` of zero beside a rating; warn on a `Review` with no `datePublished`. Provenance and moderation stay the person's
- [ ] Implement detector `ugc-governance` (3.13, a launch gate, @seo/probes `markup.ts`): fail a page carrying a comment or forum form (`<form>` with a `comment`/`reply` field or a `Comment` schema node) whose outbound links inside the comment region lack `rel="ugc"` or `nofollow`; not-applicable on a site with no user-generated content markup
- Not scheduled, because the engine gathers no evidence for them: `raw-rendered-parity`, `rendering-strategy-classifier`, `raw-rendered-crawl-diff`, `content-parity-diff`, `schema-validation-parity`, `mobile-journey-qa`, `axe-accessibility` (Phase 5, a rendered DOM); `lab-perf-budget`, `lcp-element-strategy`, `template-lab-perf`, `field-cwv-monitor` (Lighthouse/CrUX); `gsc-property-ownership`, `sitemap-submit`, `url-inspection`, `indexation-review`, `security-manual-actions`, `bing-onboarding` (Search Console/Bing); `merchant-feed-parity` (a Merchant Center feed); `staging-protection`, `migration-redirect-test`, `migration-redirects-live`, `migration-map-builder`, `indexnow-integration`, `experiment-cloaking-divergence` (a second origin, a redirect map, a key or a second identity as input); `prelaunch-baseline-snapshot`, `post-migration-monitor`, `quarterly-regression-crawl` and the rest of Phases 5-7 (a previous audit or live monitoring); every Phase 0 detector and `content-helpfulness`, `manual-a11y-evaluation`, `product-checkout-qa`, `gbp-setup`, `analytics-consent-matrix` (a person's work)
- [x] Implement the two 1.12 detectors, `parameter-crawl-space` and `faceted-nav-control` (@seo/probes `facets.ts`; @seo/crawler: `notReached` now includes URLs linked past the depth budget, which used to vanish)
- [x] Implement `http-version` (1.7) from a TLS handshake the crawl makes with the host the root document landed on, and HTTP/3 read from `Alt-Svc` (@seo/crawler: `negotiateProtocol`, `CrawlResult.protocol`; @seo/testkit: `startTlsServer`)
- [x] Implement `content-accessibility` (3.11) from markup: nameless links, images with no alt, no page language, generic link text, header-less data tables, uncaptioned video (@seo/probes `accessibility.ts`; @seo/crawler: `ExtractedLink.name`, `ExtractedImage.altExempt`, `Extracted.tables`)
- [x] Implement the two 3.9 detectors from markup: `answer-first-structure` (a page's reading matter is signposted, and its question headings have answers beneath them) and `author-date-signals` (the site's articles say who wrote them and when, with no date false on its face or stamped on every article) (@seo/probes `content.ts`; @seo/crawler: `Extracted.content`, the reading matter divided at its headings, and `Extracted.authorship`)
- [x] Give link-discovered URLs a share of the page budget alongside sitemap URLs (@seo/crawler: a lane for the walk and a lane for what the sitemaps declare, spent one request for one; `notReached` reports what is left in both) — sitemap URLs are enqueued at depth 0 ahead of every link, so on a site with a large sitemap the crawl fetches the seed and then only sitemap entries, and link-only subjects (filter URLs, orphans, click depth) are never walked. Found running the 1.12 detectors against kjell.com, whose 24k-URL sitemap took the whole 80-page budget

- [x] Spend the sitemap budget across every sitemap a site declares, not on whichever it declared first (@seo/crawler: a lane per declared sitemap, read round-robin)
- [x] Mark a response body the crawler had to cut, so a size limit of ours is never reported as a defect of theirs (@seo/crawler: `FetchResult.truncated` and `SitemapFetch.truncated`; @seo/probes: `video-sitemap` and `index-bloat` report `error`; `npm run analyze` counts fails and errors apart)
- [x] Read a sitemap to the protocol's 50 MB ceiling by parsing it as it streams, so a large sitemap is read rather than reported unobservable (@seo/crawler: `createSitemapParser`, `SITEMAP_MAX_BYTES`, `FetchOptions.onText`; IGN's 2.14 now grades and 2.1 is held on a real warning, where both were `probe-error`)
- [x] Read gzipped sitemaps (`.xml.gz` served as `application/gzip`/`application/x-gzip`) — the body was non-textual, so the document was recorded as a 200 with no URLs (@seo/crawler: `FetchOptions.gunzip`, a gzip file recognised by its first two bytes and opened as it streams; the 50 MB ceiling applies to the expanded size)
- [x] Make `maxBytes` stop the read for a page body too — the buffered path downloaded the whole response before cutting it, so the "protects against tarpits" promise on `FetchOptions.maxBytes` held only for streamed sitemaps (@seo/crawler: every body, textual or binary, is read a chunk at a time and the response cancelled at the limit; `byteLength` of a truncated body is now a lower bound)
- [x] Prototype URL analyzer with snapshot comparison (npm run analyze / npm run compare) so engine changes can be measured against live sites
- [x] Bring seven built detectors to v5.0's wording (@seo/probes: `robots-txt` and `sitemap-validity` hold an absent robots.txt or sitemap for a recorded decision, fail an unreachable or 5xx/429 robots.txt, a declared sitemap that does not answer and a file past 50,000 URLs, and warn past robots.txt's 500 KiB; `host-redirect` and `redirect-chain` warn on a second hop, and duplicate hosts naming one canonical warn; `locale-canonical` warns on same-language regional consolidation and a missing canonical, as `canonicalization` now does; `video-watch-page` leaves product and article pages alone; `ai-crawler-directive-verify` accepts an absent robots.txt that agrees, labels its requests simulations and warns on a served user-directed fetcher the policy disallows; @seo/crawler: `CrawlResult.robotsStatus`; @seo/core: `PRODUCT_TOKENS`, `simulatableAgents`; @seo/scheduler: no request under a product token's name)
- [x] Implement `crawler-fetch-limit` (v5.0 1.5): each uncompressed document measured against Googlebot's per-file limit (2 MB for non-PDF, 64 MB for PDF, per the workbook's 2026-09-08 check) with a margin — the crawler already reads byte counts, and a body it had to cut is already past the limit (@seo/probes `delivery.ts`: fail past the limit on either reading of "MB", warn between the readings and inside a 25% margin, a cut body past the limit fails, one cut inside it is `error` unless an unencoded `Content-Length` gives the size)
- [x] Report a warning on an `assisted` check as `held-by-warning`, not `awaiting-confirmation` (@seo/grader: the warning branch now runs before the assisted one) — a warning on 1.5, 1.3 or 1.6 read as "proposed, none failed", indistinguishable from clean evidence to the person asked to confirm it
- [x] Stop an HTTPS response closed behind its body from killing the process — crawling nytimes.com died on an uncaught `assert(!this.paused)` in undici's HTTP/1.1 client (@seo/crawler: `fetchPage` requests through an undici 8 `Agent` it owns rather than the global `fetch`, which dispatched through cheerio's undici 7; a body cut short is now a recorded fetch `error`; @seo/testkit: `startHttpsServer`)
- [x] Measure the CSS and JavaScript files pages load against the same 2 MB limit — Googlebot fetches each separately under it, but the crawl records their URLs and fetches none of them, so 1.5's `crawler-fetch-limit` judges documents only (@seo/crawler: `Extracted.stylesheets` from `<link rel="stylesheet">`; a bounded `asset` auxiliary pass, deduplicated across pages and capped at `MAX_ASSET_FETCHES`, mirroring the icon and external-link passes; @seo/probes: `crawler-fetch-limit` judges every linked stylesheet and script the crawl fetched alongside the document, and reports the worst verdict of the set — a page whose document passes but links an oversized script now fails)
- [x] Implement `news-sitemap` (v5.0 2.18): news metadata only on articles from the last two days, at most 1,000 news entries per file, publication name and language present — the sitemap parser already reads namespaced extensions by local name (@seo/crawler: `SitemapNews`, `CrawlResult.sitemapNews`, `SitemapFetch.newsCount` and `fetchedAt`; @seo/probes `news.ts`: fails a missing name, language, date or title, a date that is not a W3C date with a zone, one after the file was served or more than three days before it, a language that names none, an article on another origin, a file past 1,000 entries and a news-named sitemap that does not answer; warns a day past the window and on a regional language tag. theguardian.com's 430-entry feed passes)
- [x] Implement `news-article-policy` (v5.0 2.18), the publisher's half of the gate: accountable bylines, original dates, sponsorship disclosure on the articles a news sitemap lists (@seo/probes `news.ts`: an article is news when the feed lists it or it is typed NewsArticle; fails a page date more than a day from the feed's and a page the site types or files as advertising with no disclosure anywhere on it; warns on an article with no author or date in its markup and on a site with no contact or about link, and no publisher in structured data, on any crawled page. theguardian.com, bbc.com, reuters.com, independent.co.uk and nytimes.com pass)
- [x] Hold a `news-sitemap` entry on another host with a `warn`, not a `fail` — nytimes.com's feed lists cooking.nytimes.com, which Google reads once both hosts are verified to one owner in Search Console
- [x] Give `video-sitemap` (2.14) the same cross-host allowance: it still fails an entry on another host, which Google accepts from a verified owner (@seo/probes `video.ts`: an entry on another host is a `warn`, reported alongside any entry naming a page the crawl found no video on; an incomplete or robots-blocked entry still fails wherever it is)
- [x] Implement `private-response-caching` (v5.0 1.7, 4.2): a response that sets a cookie or varies by one while declaring itself publicly cacheable (@seo/probes `delivery.ts`: fails a response declaring `Cache-Control: public` that also sets a cookie or names `Cookie` in `Vary`)
- [x] Extend `pagination-crawl-path` (v5.0 1.13) to blanket page-1 canonicals and fragment-only page numbers, and `faceted-nav-control` (v5.0 1.12) to a noindex robots.txt keeps crawlers from reading (@seo/probes `site.ts`: fails a paginated page whose canonical collapses onto the unpaginated URL, and a "next"/"prev" link addressed only by a URL fragment, whether bare (`#page=2`) or trailing the same path; @seo/crawler: `Extracted.fragmentPageLinks` captures the raw hrefs `resolveUrl` drops entirely because a fragment is never sent to the server; @seo/probes `facets.ts`: a facet closed to crawling by robots.txt alone now holds the check with `warn` rather than passing it, since the crawler that cannot request the URL can also never read a noindex tag on it)
- [x] Give the engine the v5.0 workbook's second assessment, READY FOR CUTOVER: an evidence class per gate (5 planning, 43 preflight, 6 live), a release and scope revision, cutover authorization, and 5.7 recording GO only while the calculated result is GO (@seo/core `cutover.ts`: `computeCutoverReadiness`, `ReleaseRecord`, `evidenceClassOf`; `provenance-v5.0.test.ts` now reproduces the block — HOLD, 25 pre-cutover, 4 live, 8 scope errors)
- [x] Record review freshness on check states — last review, result, next review or event trigger, `reopened` — so a stale, failed or reopened review holds a gate as v5.0 requires, and an attested row stops counting once its review is overdue (@seo/core `review.ts`: `ReviewRun`, `readReview`; `computeLaunchReadiness` and `computeProgress` take `assessedAt` and stop counting a lapsed attestation; @seo/grader: assessed at `gradedAt`, so a lapsed attestation is preserved but no longer clears its gate)
- [x] Persist the release record and review runs (an append-only `review_runs` table and a release record per site, with a migration) and freeze `computeCutoverReadiness` onto `audits.readiness` beside the first assessment — today both exist only in memory, so nothing the engine stores can yet say READY FOR CUTOVER (@seo/db migration 0008: `releases`, `review_runs` with a trigger refusing UPDATE and DELETE, `audits.release_id`; @seo/grader `release.ts`: `saveRelease`, `recordReviewRun`, `loadRelease`, and `recordGrade` freezing `cutover`; @seo/scheduler: `submit({ release })`)
- [x] Decide what a review run cites for a machine-verified pass — today its evidence must equal the grader's summary line, which changes wording between engine versions and so reads as `reconcile` after an upgrade; an evidence reference built from the audit id and check id would be stable (@seo/core: `evidenceReference`, `CheckState.evidenceRef`, `citesEvidence`; @seo/grader: `recordGrade` gives every verdict it writes the reference, so a run citing `audit:<id>#<check>` stays current through a re-grade)
- [x] Give release records and review runs a way in other than code — the audit API (Phase 7) is the natural door, but until then only `saveRelease` and `recordReviewRun` write them (`npm run release -- <file.yaml>`: @seo/grader `release-file.ts`, `parseReleaseFile` and `importReleaseFile` — strict about the file, all or nothing, and re-runnable as the file grows; `scripts/release.example.yaml`)
- [ ] Serve release records and review runs through the audit API (Phase 7) on `importReleaseFile`'s rules, so the file import and the endpoint cannot disagree about what is accepted
- [x] Stop the full test suite timing out under load — with every worker busy, a different slow test (`sitemap.test.ts`'s large video sitemap, `retry.test.ts`'s limit) overruns vitest's 5 s default each run; `--maxWorkers=4` passes. Fixed: the retry timeout was a real stall in @seo/queue (a retry falling due between two clock readings was neither run nor woken for), and the genuinely slow tests now have 20 s
- [x] Implement 4.1's raw-crawl detectors, `broken-links` and `metadata-completeness` (@seo/probes `qa.ts`: `broken-links` fails an internal link to a URL answering 4xx/5xx, naming the pages that carry it, and holds on a target the crawl never fetched, timed out on or was rate-limited by; `metadata-completeness` fails an indexable page with no title and an indexation conflict — noindex or robots-disallowed and in the sitemap, noindex beside a canonical elsewhere, meta robots and X-Robots-Tag disagreeing, a canonical onto a redirecting, broken or noindex URL — and warns on a missing description, h1 or canonical, and on a canonical that redirects straight back to its page)
- [x] Check external link targets for `broken-links` (4.1) — it used to count off-site links and leave them alone, because the crawl holds one host's permission; a bounded, paced auxiliary pass per external host now answers "no broken links" in full (@seo/crawler `crawl.ts`: an `external-link` auxiliary reason, fetched after the walk from lanes per external host, at most 3 per host and 30 requests in total, round-robin like `loadSitemaps`; @seo/probes `qa.ts`: `broken-links` reads `crawl.auxiliary` for a checked external target, fails a broken one alongside internal findings, and holds an external target the auxiliary pass did not reach the same way it already held an unfetched internal one)
- [x] Implement `trust-pages-presence` (3.4, a launch gate): About, Contact, a privacy policy and terms are linked from somewhere the crawl read, and the link answers rather than 404s or 5xx's (@seo/probes `content.ts`: matched by whole anchor text or whole path segment, the same rule `news-article-policy`'s contact/about matching already uses; a category with no matching link anywhere holds with `warn` for the 0.9 scope review, a matching link the crawl reached and found broken fails outright, and a matching link the crawl never fetched holds with `warn` rather than either)
- [ ] Refuse a pass on a failed threshold by exception alone (v5.0 1.5, 4.2, 4.5, 4.7): an attestation may record a decision, not overturn a failed measurement

## Phase 5 — Rendered Crawl
- [ ] Implement JavaScript rendering in @seo/crawler (renderMode column exists in schema but not used)
- [ ] Add dual-crawl (raw + rendered) logic to compare HTML vs. rendered content
- [ ] Detect and report rendering strategy mismatches in @seo/probes

## Phase 6 — External Content Storage
- [ ] Implement content-addressing system (S3/GCS integration) for page bodies
- [ ] Map page body hashes to storage keys in database (body_key column prepared in schema)
- [ ] Build blob retrieval client for reconstructing archived crawls
- [ ] Add batch operations for uploading and purging stored content

## Phase 7 — Audit API
- [ ] Create HTTP server entry point (currently library-only, no apps/ yet)
- [ ] Build site management endpoints (create, list, update, delete)
- [ ] Implement audit lifecycle endpoints (create, status, result retrieval)
- [ ] Add check attestation endpoint for recording human decisions
- [ ] Implement readiness calculation and score retrieval

## Phase 8 — Dashboard
- [ ] Build web UI for audit results and historical trend viewing
- [ ] Implement check evidence drill-down (trace verdict to probes to observations)
- [ ] Add audit comparison across sites and time
- [ ] Create attestation interface for confirming checks

## Blocked

## Decisions
- 2026-09-18: implemented `publisher-discover-readiness` (2.16) against two markup-only signals on pages the crawl types as articles (the same `declaresArticle` reading `author-date-signals` uses) — an `og:image` whose declared `og:image:width` falls under Discover's 1200px minimum, and a `max-image-preview` directive (meta robots or `X-Robots-Tag`, matching the combined-directive pattern already used for indexation checks) set to anything other than `large` — because 2.16's `doneWhen` is otherwise a Search Console record (actual indexed evidence, Preferred Sources adoption and current eligibility) no crawl can produce. An `og:image` with no declared width is left unjudged on that signal rather than assumed too small, since Discover's own minimum is a property of the declaration, not a default a missing tag implies. Built as a site probe, like `author-date-signals`, because "not-applicable on a site with no article pages" is a whole-crawl verdict, not a per-page one
- 2026-09-18: implemented `consent-mode-config` (2.6) against one ordering signal only — whether an inline `gtag('consent','default',…)` call appears before or after the gtag.js/gtm.js script tag in document order — because 2.6's `doneWhen` is otherwise a runtime behaviour review (mode selection, per-region scope, choice persistence and withdrawal across first visit, accept, reject, partial consent and return visits) that only a rendered DOM under repeated navigation could exercise, which is why the check stays `assisted`. Gave `@seo/crawler` extraction `Extracted.scriptTags`, every `<script>` interleaved in document order regardless of `src`, because the existing `scripts`/`inlineScripts` fields split external from inline and so cannot say which ran first — exactly the ordering 2.6 turns on. A page with no Google tag at all is `not-applicable`; one with a tag, no consent default anywhere and no script from a known CMP host is held with `warn` rather than failed, since the absence could equally mean the page is genuinely out of the 0.9 consent scope, which only a person can confirm
- 2026-09-18: implemented `analytics-implementation` (2.5) against two machine-observable signals only — a `gtag/js?id=`/`gtm.js?id=` script tag pasted in twice on one page, and pages whose full id sets (script-tag ids unioned with `G-`/`GT-`/`GTM-` literals found in inline scripts) disagree — because 2.5's `doneWhen` is otherwise a live-payload and ownership review (test/internal traffic, PII redaction, deduplicated transaction ids, the optional GA4/Search Console link) this engine has no input for. A duplicate counts only when the *same script tag* repeats: the standard GA4 install already cites its id once in the script `src` and once more in an inline `gtag('config', …)` call, and counting that ordinary pair would fail every properly configured site. A page with no id at all is left out of the cross-page agreement comparison, since a site can legitimately tag only some of its pages; only "no tag anywhere" reads as a finding, and it warns rather than fails, per 2.5's own applicability to sites using analytics. Gave `@seo/crawler` extraction `Extracted.inlineScripts` (raw `<script>` text with no `src`) for this and any future inline-script detector to read
- 2026-09-18: implemented `launch-content-completeness` (3.7) against two signals only — placeholder text still on the page (`lorem ipsum`, `coming soon`, `[insert`, and the bounded tokens `TODO`/`TBD`/`xxx`) and reading matter that is empty or under 50 words — because 3.7's `doneWhen` is otherwise an inventory-and-approval question (which URLs are launch-critical, whether their copy, media, links and conversion path were signed off) that only the 0.9/0.3 inventory and a person's approval can answer. Unfinished copy is the one defect a machine can name outright regardless of which URLs the inventory marks critical, so scope was left at the whole crawl rather than narrowed. Also gave `@seo/crawler` extraction a joined, whitespace-safe `content.text` (`Extracted.content.text`) for this and any future page-text detector to read, alongside the existing per-section word counts — built the same way the byline text already was, by joining text nodes with spaces, because cheerio's own `.text()` concatenates adjacent elements with nothing between them and would silently fuse a heading and the paragraph after it into one unmatchable word
- 2026-09-18: implemented `cannibalization` (3.10) against one signal only — two or more indexable, self-canonical pages presenting an identical `<title>` or `<h1>` after trimming and case-folding — because 3.10's own `doneWhen` asks a person to weigh user need, entity and search role, none of which a crawl reads; identical titles or headings under separate, undecided addresses is the one shape that is never itself a recorded decision. A page with no `rel=canonical` is treated as self-canonical (nothing on it says otherwise), matching the default `product-variant-canonical` already reads; a page canonicalizing onto the one it would otherwise duplicate, or marked noindex, is left out of the comparison entirely, since either is a decision already on record. Fails outright rather than warns, since 3.10 is `assisted` and a fail here is a proposal for a person to confirm, per the check's own caution against assuming every overlap is harmful
- 2026-09-18: split the detector work into one roadmap checkbox per detector, and closed the running batch line as done. The roadmap runner (`run-roadmap.ps1`) hands the first unchecked line to a fresh 80-turn session, and a line that names all 65 remaining detectors is not a task that session can finish — the last attempt read the whole codebase, implemented nothing it could commit, and was marked stuck. Each new line names the detector, its check, the file, and what fails on its face versus what stays a person's, so the session can start writing rather than deciding scope
- 2026-09-18: implemented `domain-expiry-rdap` (1.18) through rdap.org rather than IANA's bootstrap registry, one request instead of two, and approximated the registrable domain as the host's last two labels rather than shipping the public suffix list — a wrong guess (`example.co.uk` → `co.uk`) is answered 404 and recorded as a lookup `error`, never as a finding, so the approximation fails closed. Only expiry moves the verdict: registrar and lock status are recorded for the review 1.18 still needs, because many properly run domains carry no lock status in RDAP at all
- 2026-09-18: implemented `indexability-matrix-reconciliation` (4.2) against one signal only — a same-site stylesheet or script the page links that robots.txt keeps Googlebot from fetching — because 4.2's `doneWhen` is otherwise a reconciliation against the 0.3 URL matrix (an approved plan this engine holds no input for) across cache layers, cookies, locales and private-route authorization that a raw crawl cannot test. "Essential rendering resources" is the one clause a crawl already has the evidence for: it reads every page's linked CSS/JS from `Extracted.stylesheets`/`Extracted.scripts` (gathered for `crawler-fetch-limit`, 1.5) against the same `robots.txt` the crawl already parsed, so the finding costs no new request. An off-site asset passes, the same reasoning as `image-discoverability`: this site's robots.txt has nothing to say about a third party's
- 2026-09-17: implemented `image-discoverability` (2.3) against one signal only — a page's own og:image or twitter:image, resolved same-site, that robots.txt keeps Googlebot-Image from fetching — because 2.3's `doneWhen` immediately qualifies Google's 1,200px/300,000px/16:9 recommendations as "record decisions against", "do not treat as universal", which is the corpus's own way of saying a machine must not fail on them. Crawlability is the one requirement stated as a requirement rather than a recommendation, and the only one a raw crawl (no image bytes fetched, no pixel dimensions) can name outright. An image hosted off-site is left `pass`: this site's robots.txt has nothing to say about a third party's, and the engine reads only its own
- 2026-09-17: implemented `paywall-access-model` (2.15) against the markup a gated page can ship on its own — `isAccessibleForFree`, and `cssSelector` on each `hasPart` entry where the page names a partial preview — because 2.15's `doneWhen` is otherwise a live access-parity test: an authorized reader, a logged-out client and a verified crawler compared against the approved policy, none of which a single raw crawl can stand in for. A page that declares neither `isAccessibleForFree` nor `hasPart` at all is left `not-applicable` rather than failed, since that is exactly what 1.8's deliberately-excluded gated content also looks like from outside — the engine cannot tell "no paywall markup because there is no gate" from "no paywall markup because the gate was never built". A node that opts in via `hasPart` sections but never declares its own `isAccessibleForFree` fails outright, and a `hasPart` entry missing either `cssSelector` or its own `isAccessibleForFree` fails the same way: each is a required property Google's own paywalled-content guide names, not a policy judgement
- 2026-09-17: implemented `outbound-link-qualification` (3.13) against one signal only — an outbound link on a page the site itself declares advertising (typed `AdvertiserContentArticle`, or filed under a `/sponsored/`-style path segment, the same declaration `news-article-policy` already reads for its own disclosure finding) with no `sponsored`, `ugc` or `nofollow` relationship — because 3.13's `doneWhen` is otherwise a moderation and provenance review no crawl can see: which arrangements are genuine, whether reviews are real, who escalates a policy notice. A page nobody has typed or filed as advertising is not a defect this detector can name, since whether an unmarked page is paid for is exactly what `news-article-policy`'s companion decision (2026-09-17, above) already found unobservable. 3.13 stays ungraded end to end until `ugc-governance` and `review-integrity` exist alongside it
- 2026-09-17: implemented `locale-content-parity` (3.12) against one signal only — a reciprocal hreflang pair whose primary language subtags differ but whose page headings are word-for-word identical — because 3.12's own "done when" asks a qualified human to judge intent, terminology, currency and legal scope, none of which a crawl can read. Identical headings under different declared languages is the one defect false on its face: the page has not been localized at all, whatever a human review would otherwise find. Compared against the page's *self-declared* hreflang code (falling back to its `lang` attribute), not the other page's claim about it, so a mislabelled cluster is `hreflang-implementation`'s finding, not this one's; same-language regional pairs (`en-US`/`en-GB`) are left uncompared, since consolidating those is `locale-canonical`'s allowed case, not a localization defect. A pair with fewer than two headings on either side is held rather than judged, since there is not enough text to say anything
- 2026-09-17: implemented `product-schema` (2.11) against the page's own Product/ProductGroup JSON-LD only, and left `merchant-feed-parity` unimplemented, because 2.11's `doneWhen` splits into "shipped markup" and "multiple data sources agree" — the second needs a Merchant Center feed this engine does not fetch, and folding a feed comparison into a markup-completeness detector would make one detector's pass depend on evidence it cannot itself gather. Required-field defects (no Product node at all, a missing `name`, none of `offers`/`review`/`aggregateRating`, an offer with no `price` or `priceCurrency`) fail; recommended gaps (no `image`, no declared `availability`, no `gtin`/`mpn`/brand+sku identifier) warn for the person 2.11's `assisted` tier already asks to confirm the rest
- 2026-09-17: scoped `trust-pages-presence` (3.4) to four near-universal categories — About, Contact, a privacy policy, terms — rather than the corpus item's full list ("privacy, terms, company, returns, accessibility or consumer-information pages required by the approved 0.9 scope"), because which of those a given site owes its visitors is the 0.9 scope decision itself, not something a crawl can read off the site. A missing category holds with `warn` for that reason; a linked page the crawl reached and found broken fails outright regardless of scope, since a dead link is a defect whatever pages 0.9 requires
- 2026-09-17: downgraded `faceted-nav-control`'s clean pass on robots.txt-only exclusion to a `warn`, because every URL in `blockedByRobots` is by construction one the crawl discovered through a link — exactly the case Google documents indexing anyway, without a snippet, and exactly the case where a crawler forbidden from requesting the page can never confirm whether a noindex tag is there to read. Disallow alone was never proof of exclusion; it only looked like proof because nothing checked
- 2026-09-17: read a "next"/"prev" link whose only difference from the current page is its fragment as the same defect whether the href is bare (`#page=2`) or trails the current path (`/blog#page=2`), because a fragment is never sent in a request either way — added `Extracted.fragmentPageLinks` for the bare case only, since `resolveUrl` already drops it before a probe could see it, while the trailing case survives into `links` and needed no crawler change
- 2026-09-17: failed a paginated page's canonical collapsing onto the unpaginated URL outright, the same severity as noindex, rather than warning, because both tell a crawler the same thing — index page one, not this one — and 1.13 asks that a paginated page be indexed as itself, not that it merely answer 200
- 2026-09-17: had `private-response-caching` (1.7, 4.2) fire only on an explicit `Cache-Control: public` directive, not on the mere absence of `private`/`no-store`, because "declaring itself publicly cacheable" is what the corpus item names and the RFC 9111 default (a shared cache *may* store an unlabelled response) is a policy question a crawl cannot settle; scoping to the explicit directive keeps the check deterministic and matches routes like the fixture's plain `max-age=60`, which stays `not-applicable`
- 2026-09-17: failed rather than warned both the cookie-setting and the Vary:-Cookie case, unlike most `assisted` checks in 1.7, because each is a defect a machine can name outright from headers alone — a shared cache may replay one visitor's cookie to another, or serve personalised content under a directive meant for content that is the same for everyone — with no editorial judgement left for a person to make
- 2026-09-17: made `crawler-fetch-limit` report the worst verdict across the document and its linked stylesheets/scripts, rather than a separate check per asset, because 1.5 is one check id and the corpus model gives one probe one observation per page; a page whose document passes but links an oversized script now fails the page's 1.5, with the offending asset named in the summary and every asset's own verdict kept in `data.assets` for the report to unpack
- 2026-09-17: left an asset the crawl never fetched out of the verdict (`assetsUnchecked` count only, no downgrade), rather than holding the page with a `warn` the way `broken-links` holds an unfetched internal link, because the asset budget (`MAX_ASSET_FETCHES = 20`, deduplicated across the whole crawl) is expected to miss assets on a large site by design, and warning every such page would make 1.5 unpassable wherever CSS/JS repeats across many pages — unlike `broken-links`, where an unfetched target is the one fact the check exists to establish
- 2026-09-17: deduplicated stylesheet/script URLs across the whole crawl before fetching, capped at 20 total, rather than per page like icons (3) or per host like external links (3/host, 30 total), because assets are shared across a site's templates far more than they vary — a handful of bundles serve most pages — so one crawl-wide cap reaches nearly every distinct file a per-page cap would refetch redundantly
- 2026-09-17: checked an external link's target with one auxiliary request per distinct target rather than by host alone, capped at 3 per host and 30 in total, because "no broken links" is a claim about the targets a page actually cites, and a per-host sample would let a genuinely dead URL hide behind a live one on the same host. The cap exists so one page's forty links to a single rival site cannot crowd out every other external host out of the same budget
- 2026-09-17: held `broken-links` on an external target the auxiliary pass did not reach, rather than leaving it uncounted as before, by the same reasoning already applied to an unfetched internal target (2026-09-17, below): a budget of ours is not evidence about the rest, and only checking it made the omission visible instead of silent
- 2026-09-17: put the external-link fetch in the crawl loop (`crawl.ts`), not in the probe, following the existing auxiliary-request rule — politeness is owed to a host, and the crawl loop is the only thing that knows what has been promised to it; a probe fetching on its own would be a second, unmetered visitor to every site a page links to
- 2026-09-17: recompute a restored job's lane from its payload (`JobQueueOptions.reviveLane`) rather than migrate the `jobs` table or version the `lane` column, because the lane is already a pure function of data the payload carries (`AuditJob.origin`) — recomputing it on every restore keeps old-format rows self-healing across the deploy that changed the rule, with no backfill and no format to track going forward
- 2026-09-17: made `broken-links` hold (`warn`) whenever a linked internal URL went unfetched, so on any real site larger than the page budget it cannot pass, because "no broken links" is a claim about every link and a budget of ours is not evidence about the rest; theguardian.com, docs.python.org and kjell.com at 40 pages each held with 500–1,200 targets unfetched and none broken
- 2026-09-17: held rather than failed a canonical that redirects straight back to its page, because theguardian.com's `/europe` names `/`, which redirects to `/europe` from Europe and elsewhere for other visitors: from one vantage the page is its own canonical, and only a person knows which visitor the site meant
- 2026-09-17: fixed the queue rather than raising the retry test’s timeout, because a job with a 1 ms backoff overrunning 5 s is not slowness: `#take` and `#scheduleWake` read the clock separately, and a retry due between the readings was skipped by both and never ran. One reading per pump now serves both
- 2026-09-17: raised vitest’s `testTimeout` to 20 s for the whole suite rather than capping workers, because the crawl, sitemap and Postgres tests take up to 3 s alone and over 11 s on a saturated machine; fewer workers would slow every run to spare the busy ones, and a stalled test still fails
- 2026-09-17: let a review run cite a machine verdict by `audit:<auditId>#<checkId>` as well as by its summary text, and gave the reference only to engine-written rows, because the summary is report wording that a newer engine rewords, while an attested row's evidence is a person's own words and is what their reviewer should cite; the reference is derived, so it needs no column
- 2026-09-17: enforced the review log's append-only rule with a trigger rather than by convention, because v5.0 names deleting historic runs to improve readiness as the thing never to do, and only the database can refuse every client. A site's deletion still cascades: the trigger lets a DELETE through when it arrives from the foreign key's referential trigger (`pg_trigger_depth() > 1`)
- 2026-09-17: refused, at write time, any review run the assessment would read as an input error, because a stored bad run holds its gate permanently in an append-only log with no way to correct it but deleting the site
- 2026-09-17: keyed review runs by site, not by release or audit, because a run names its release as text and runs for other releases are history the assessment reads past; the release table's id is what an audit points at
- 2026-09-17: left the assessment time off the `releases` row, because it is when a reading is taken, not a fact about the release; the grader assesses at `gradedAt`
- 2026-09-17: derived a gate's evidence class from its phase (0 planning, 5 live, the rest preflight) rather than compiling it into the corpus YAML, because the v5.0 workbook's own scope check derives it the same way and flags any register row that disagrees, so a stored copy could only ever be wrong
- 2026-09-17: collapsed the workbook's GateEvidence record into the latest current review run, because the workbook requires the two to match field for field; one record cannot disagree with itself, and a mismatched copy was the only failure the second one could add
- 2026-09-17: defaulted a gate's criterion revision to the corpus version when the release names none, because a review taken against v5.0's wording is exactly what an audit pinned to 5.0 can check, and a release that revises one criterion can say so per id
- 2026-09-17: made freshness depend on an explicit `assessedAt` and never on the wall clock, as the workbook's "Assessment UTC" does, so the same states always assess the same way and a frozen report can be recomputed; the grader assesses at `gradedAt`
- 2026-09-17: read an attested state with no expiry as lapsed once an assessment time is given, because the schema already refuses such a row and an attestation nobody bounded is not one a launch should rest on
- 2026-09-17: failed a sponsored page in `news-article-policy` only when the site itself declares it advertising — typed `AdvertiserContentArticle`, or filed under a whole path segment such as `/sponsored/` or `/partner-content/` — and nothing on it says so. Whether an unmarked page is paid for is not observable, and a disclosure word anywhere on the page, an ad slot's "Advertisement" included, counts; matched without word boundaries, because the extracted text runs a headline into the label under it ("A storyPaid post")
- 2026-09-17: matched contact and about links by whole anchor text or whole path segment, never by a word inside one, because a front page is a list of headlines and "What we know about the storm" is not an about page
- 2026-09-17: allowed a page date and a feed date a day apart before failing, on top of the date-only feed's whole-day span, because a page's `datePublished` in the newsroom's zone and a feed written in UTC routinely straddle midnight; a story re-dated into the two-day window is days off, not hours
- 2026-09-17: held a news sitemap entry on another host with a `warn` rather than failing it, after nytimes.com's feed listed cooking.nytimes.com: Google accepts cross-host sitemap entries once the owner verifies both hosts in Search Console, which a crawl cannot see
- 2026-09-15: fixed the nytimes.com crash by giving @seo/crawler its own undici 8 client, not by changing how `fetchPage` reads bodies, because the reader was not the cause: the crash is in undici 7.29's HTTP/1.1 parser, which asserts when a TLS server's end of stream arrives while backpressure has paused it — a gzip-encoded body with `Connection: close` is enough, and a plain `fetch` reading a page whole crashes the same way. The global `fetch` reached undici 7.29 because cheerio imports undici, which installs itself as the global dispatcher; undici 8 drains the paused parser on end instead. The crash could not be caught and recorded as a fetch error, since it is thrown from a socket event outside any promise `fetchPage` awaits; what can be recorded now is — a body the connection cut short fails the fetch with an `error`
- 2026-09-15: had the HTTPS test install cheerio's undici as the global dispatcher before it runs, because under vitest Node's bundled dispatcher is already in place when cheerio loads, so the suite passed against the very crash it was written for. With the dispatcher put back as a crawler process has it, the test fails on master with the production assertion
- 2026-09-14: judged a news entry's age against the moment its sitemap was served (`SitemapFetch.fetchedAt`), not the clock when the probe runs, so a re-run over a stored crawl says what it said the first time; read a date without a time at the time zone most favourable to the site (its last moment in UTC−12 for age, its first in UTC+14 for a future date), and held entries up to a day past the two-day window with a `warn`, because a feed regenerated and cached, or pruned once a day, carries them and Google tolerates it — three days on, the feed is not being pruned
- 2026-09-14: warned rather than failed on a `news:language` that is a valid BCP 47 tag with a region (`en-GB`): Google documents a bare ISO 639 code with only `zh-cn` and `zh-tw` as regional forms, so the tag is outside the format, but it names a real language and is not false on its face. A two-letter code that names no language (`jp`, withdrawn `iw`) fails
- 2026-09-14: kept `crawler-fetch-limit`'s between-readings band a `warn` rather than a `fail`, and fixed the grader instead, because the case for failing was visibility, not truth: on an assisted check the grader tested `assisted` before `warn`, so a warning was summarised as "none failed" and the only thing that made it worth failing was that nobody would see it. Status and coverage are unchanged (`in-progress`, `unknown`), so readiness does not move; only the basis and summary do
- 2026-09-14: had `crawler-fetch-limit` fail only past 2 MiB (64 MiB for a PDF) and warn between 2,000,000 bytes and that, because Google writes "2MB" without saying which megabyte, and the first live run found en.wikipedia.org/wiki/World_War_II at 2,068,037 bytes — past one reading, inside the other. Failing it would state as fact a cut Google may not make; passing it would ignore that 1.5 asks for a margin, which is gone on either reading
- 2026-09-14: set `crawler-fetch-limit`'s margin at 75% of the stricter reading (1.5 MB, 48 MB for a PDF), because v5.0 1.5 asks for "a margin" without a figure, and a quarter of the limit is enough for a template to grow between audits without the next one finding it cut
- 2026-09-14: judged every successful response `crawler-fetch-limit` could call a document — HTML, text, XML, JSON, office files, PDFs — and left image, video, audio, font and opaque binary types `not-applicable`, since those are fetched by other crawlers under their own terms or not indexed as text at all. A body the crawler cut is measured by an unencoded `Content-Length` when the server sent one, because behind `Content-Encoding` that header is the compressed size and says nothing about the limit, which Google applies uncompressed
- 2026-09-14: the maintainer signed off the v5.0 triage as drafted, all 98 rows, including the fifteen moves from `automated` to `assisted`; v5.0 now has 26 automated checks, and a machine can pass only those
- 2026-09-14: made an absent robots.txt a `warn`, where it was a `fail`, because v5.0 2.1 calls a missing robots.txt "an explicit policy decision, not automatic deindexing" — a 404 opens the whole site, which is often what is meant. What still fails is a robots.txt crawlers read as "stay out": every path disallowed, or a 5xx, a 429 or no answer at all, which Google treats as a server error. That needed the robots.txt status on `CrawlResult`, since `robots.absent` read a 404 and a timeout the same way
- 2026-09-14: stopped asking robots.txt for a Sitemap line and made a site with no sitemap a `warn`, because v5.0 makes the sitemap optional and asks instead for the omission decision and internal discovery; a sitemap robots.txt names and that does not answer still fails, since the site believes it publishes one, and so does a file past the protocol's 50,000 URLs, which v5.0 now spells out
- 2026-09-14: made a second redirect hop a `warn` in `host-redirect` and `redirect-chain`, because v5.0 asks for one hop "preferably" and "where feasible" and has external chains "assessed by impact" — a judgement about a chain, not a verdict on its length. Loops, http landings and 4xx/5xx variants still fail
- 2026-09-14: let host variants that land on different URLs `warn` when every landing page declares one canonical, and fail otherwise, because v5.0 1.6 allows "an intentional retained duplicate-host exception" with "consistent canonical behavior". The canonical is read from the variant's own response body, which the crawl already holds, so no request is added
- 2026-09-14: split `locale-canonical`'s cross-locale canonical by language: onto another language still fails, since v5.0 says hreflang never justifies "cross-language canonical collapse"; onto another region of the same language warns, since v5.0 0.7 and 1.14 allow documented same-language regional consolidation, and only the locale plan can say it was chosen. Languages are compared on the primary subtag, and `x-default` matches nothing. A locale variant with no canonical at all became a `warn`, and so did any page with none in `canonicalization`, because v5.0 1.3 asks for self-canonicals "where appropriate" rather than everywhere, and whether a page has a duplicate needing one is not something the page itself shows
- 2026-09-14: had `video-watch-page` leave pages that declare themselves products or articles alone, superseding the 2026-09-08 decision to judge every page carrying a video, because v5.0 2.14 says "a product/article page with supplemental video is not claimed as a dedicated watch page". The declaration is read the way the commerce and content detectors already read it, so one page cannot be a product to one detector and a watch page to another
- 2026-09-14: stopped sending a request under a product token's name — `Google-Extended`, `Applebot-Extended` — because v5.0 2.9 says Google-Extended "is tested as a product token, not a fictitious fetcher": nothing fetches as it, so a request under that name tests a visitor that does not exist. Such tokens are checked in robots.txt only, and a policy naming nothing else passes on robots.txt alone
- 2026-09-14: labelled `ai-crawler-directive-verify`'s requests as simulations in its evidence and summary, because v5.0 2.9 separates rule simulations from identity-verified requests, and a user-agent string is only the first; genuine crawler traffic is identified by the provider's IP or DNS method in logs a crawl cannot see, which is also why 2.9 is `assisted` in v5.0
- 2026-09-14: made a served user-directed fetcher the policy disallows (`ChatGPT-User`, `Perplexity-User`) a `warn`, an exception to the 2026-09-08 rule that a disallowed crawler receiving a 200 is normal, because v5.0 records that OpenAI says robots may not apply to ChatGPT-User and Perplexity-User generally ignores it — for those, robots.txt enforces nothing, and only an edge rule would. An absent robots.txt no longer fails the check by itself either: it allows everything, which agrees with a policy that does
- 2026-09-14: fixed a race in the recovery test for adoption rather than leaving it flaky: it read the `jobs` table the moment the audit row read `complete`, but the handler returns before the queue removes the job, so under the full suite's load the row was still there. It now drains the survivor first, as its sibling test does
- 2026-09-14: refused to audit a site whose flags were declared against another corpus version than the audit's, rather than mapping new flags from old ones, because v5.0 made five universal checks conditional (0.5, 2.2, 3.9, 6.8, 7.8) and `resolveScope` reads a missing flag on a filled-in profile as a decision — a v4.4 profile reading `ecommerce` would have had 2.2, image alt text and a launch gate, excused on its first v5.0 audit with a rationale that reads deliberate. Only a person can say whether the site has what the new conditions ask about; it is also the workbook's own rule that a new inventory needs a fresh scope decision
- 2026-09-14: backfilled `4.4` onto every profile with flags in the migration, because 4.4 was the only corpus the engine had held when any of them were written, so the record is true, and leaving it null would have stopped every existing v4.4 audit for a question already answered. An empty profile needs no version, since it states nothing and already leaves conditional checks at `review`
- 2026-09-14: went from corpus v4.4 straight to v5.0 and skipped 4.5, because no audit pins 4.5 and every version on disk is one the engine must keep able to re-grade; a version nobody will run is a maintenance cost with no report behind it
- 2026-09-14: made the engine mirror the workbook — a check id means what the workbook's current version says it means — so where v5.0 gave 3.9 a new requirement (batch and AI-generated publishing), the two content detectors followed their subject into 3.5 rather than the id being renumbered. The workbook's own rule ("never reuse an old control ID for a different requirement") is broken by that row; the engine follows the workbook as issued and `npm run compare` already flags a corpus change between snapshots
- 2026-09-14: made detectors follow the current methodology rather than versioning them. Stored probe results keep an old audit explainable and re-gradable, which is what version pinning exists for; versioning each detector by corpus would double every detector whose rules moved, for audits nobody is going to run against v4.4 again. `CURRENT_CORPUS_VERSION` is the one place that says which methodology that is
- 2026-09-14: cited v5.0 sources by the workbook's stable Source IDs rather than the fuzzy topic match v4.4 needed, and made an id the Sources sheet does not hold a compile error, because v5.0 notes say "Source IDs: SRC006" where v4.4 said "See Sources: X" — the old matcher would have compiled 97 of 98 checks with no citations and exited cleanly
- 2026-09-14: exported the workbook's Checklist, Sources, Progress and How to use sheets as displayed values, with Excel date serials in Sources rendered as ISO dates, and had the export refuse any cell containing a tab or line break rather than rewrite it, because the compiler reads the TSV with a plain split
- 2026-09-14: triaged v5.0 by v4.4's rule read against the new wording, and moved fifteen rows to `assisted` where the "Done when" itself now asks a person for a record — an owner for unavailable data, a reason for an ineligible case, reviewed evidence for a retained duplicate, an impact assessment for an external chain. Wording that names a decision taken elsewhere ("the approved matrix", "the chosen fallback") stays an input. Drafted, not signed off: the table says so and the sign-off is its own Phase 3 item
- 2026-09-14: mapped v5.0's "sites with on-page images" (2.2) to `images`, `media` or `image-search`, because a site that declared meaningful media or image search has on-page images by definition, and a gate should not leave scope on a flag vocabulary change the site never saw
- 2026-09-12: had a live worker ask the store for abandoned work on every heartbeat (`JobStore.adopt`), rather than only in `recover()` on the way up. The lease already said when nobody alive held a job; nothing acted on it between restarts, so a worker that died at noon left its backlog sitting there while healthy workers watched the same table. Adoption is the same act as recovery at a different moment, which is why both go through one `#restore`
- 2026-09-12: excluded a worker's own rows from adoption, whatever their claim says. They are already that queue's to track, and the write that makes an adopted row `queued` — so it stops holding its lane — would stamp a job the worker is running as waiting, which is the one thing lane exclusion cannot survive
- 2026-09-12: capped adoption at 25 jobs a beat, so two live workers divide a dead one's backlog instead of whichever woke first taking all of it. It costs nothing: a worker runs `concurrency` at a time and the rest would sit in its memory either way
- 2026-09-12: refused to adopt while paused, because a paused worker would take work it has no intention of running and then hold it — renewed, and so invisible to every other worker — for as long as it stayed paused
- 2026-09-12: let a queue holding nothing keep beating, which it used to stop doing, since the workers best placed to take on abandoned work are the idle ones and an idle one had no timer left. The watch timer is unref'd, so a process with nothing outstanding still exits when it is done rather than being held open by a queue looking for other people's work
- 2026-09-12: had @seo/scheduler reset an adopted audit's row to `pending` from the `adopted` event, best-effort, because the row still reads `running` from a worker that is not running it; it is `recover`'s reset arriving by another route, and a failed update must not stop this process taking the work on
- 2026-09-12: gave the page budget two lanes — the walk and what the sitemaps declare — spent one request for one, superseding the single queue that enqueued every sitemap URL at depth 0 ahead of every link. Measured with `npm run analyze` against kjell.com at 80 pages: before, all 80 pages fetched were sitemap product URLs and not one carried a query string; after, 40 were walked — the seed, category and outlet listings, `?outlet=a` filter variants, the `/no` locale — and `canonicalization` went from 1 failure to 40, because a page with no rel=canonical here is exactly the kind of page no sitemap lists. 2.8 moved from passed to held for the same reason: it was clear only because the page that warns had never been fetched
- 2026-09-12: let a lane that runs dry hand what is left of the budget to the other, rather than reserving half for each, so a site with no sitemap is walked exactly as it was and a sitemap smaller than its share is still read whole — the split only binds when both lanes have more than the budget can take, which is the only case that was ever broken
- 2026-09-12: kept the walk first in the rotation, so the seed is still the first page fetched: the root document is what the icon requests, the TLS handshake and the sink’s first row are taken from, and none of that should move because of how the rest of the budget is divided
- 2026-09-12: restated the sink’s ordering guarantee as "a page arrives after the page it was linked from" rather than "breadth-first from seeds", because interleaving the lanes breaks the second and not the first — a page’s links are enqueued only once it has been fetched and awaited, which is the property `discoveredFromId` actually resolves against
- 2026-09-11: moved 3.9 from `automated` to `assisted`, signed off by the maintainer, under the same 2026-09-04 rule that moved 3.11: its "Done when" asks that content *answers* its main query, that claims *needing* support carry sources, and that bylines and dates appear where readers would *reasonably expect* them — three editorial judgements, where markup shows only that headings, bylines and dates exist. Edited in v4.4 rather than cut as a new version because neither detector existed, so no audit had ever graded 3.9. It is not a launch gate, so less rode on it than on 3.11, but a machine pass is a clearance either way. Automated checks: 42 to 41
- 2026-09-11: split 3.9 by how many pages each question needs: `answer-first-structure` reads one page's reading matter, `author-date-signals` reads the site's articles together, because the failure the corpus names outright — bylines and dates "as site-wide boilerplate" — is a fact about many pages that no page can see about itself, which is why 2.7's detector is site-scoped too
- 2026-09-11: identified an article only by what the page declares — a schema.org Article-family type in structured data, or `og:type=article` with an `article:published_time` — and never by `og:type` alone, because WordPress SEO plugins set that on every singular page, the contact page included, and judging bylines there would bury the finding under pages nobody expects one on. The first draft took structured data only, and `npm run analyze` against smashingmagazine.com and wordpress.org/news found both publish without any JSON-LD and declare every article as an Open Graph article with a publication time; they read `not-applicable`. A site whose articles declare neither still does, and whether they ought to is a person's question
- 2026-09-11: asked for an author only on pages typed as articles in structured data, while judging the dates of Open Graph articles too, because an Open Graph article with a publication time is also what a static-site template writes on every dated page: the second live run found Smashing Magazine's ebook-bundle product pages declared that way, and seven "names no author" warnings on shop pages were the byline-forcing the corpus tells sites not to do
- 2026-09-11: failed only dates that are false on their face — a value that is not a date (an unrendered `{{ post.updated }}`), a modification more than a day before publication, a date more than a day after the server's own `Date` header — and made a missing byline, a missing date and a stamp a `warn`, because whether readers expect a byline on a given article is the judgement 3.9 leaves to a person. The day of slack keeps a timezone from reading as a contradiction; the `Date` header rather than the analysing machine's clock lets a replayed crawl reach the verdict it reached the day it ran
- 2026-09-11: called a date a template's stamp only when one value carrying a time of day is shared by at least five articles and four in five of those declaring one, because a news site publishes many stories on one date, while one value identical to the second across most of an archive is a build, not an editor
- 2026-09-11: had `answer-first-structure` never fail: 600 words under no subheading and a question heading with nothing beneath it are `warn`s, because the second is also what an FAQ whose answers arrive by script looks like in a raw crawl (Phase 5), and whether the text answers anything is the question the check leaves to a person. Only headings in the reading matter count — the main landmark, a lone article, or the body without navigation, asides and page chrome — since a menu's headings signpost nothing on the page
- 2026-09-11: counted a heading as a question only by its question mark, because "How to fit a tap" is a task answered by whatever steps sit beneath it, and reading interrogative words as questions would demand an answer paragraph from headings that already have one in the form of their subsections
- 2026-09-11: moved 3.11 from `automated` to `assisted`, by the triage's own 2026-09-04 rule that a row is automated only when its "Done when" closes on observation alone — 3.11's asks that media alternatives are *accurate* and content understandable, and markup shows only that they exist. With one detector behind the check, keeping it automated would have let a markup pass clear a launch gate nobody had read, which is the 2026-09-07 `ai-crawler-directive-verify` problem again. Edited in v4.4 rather than cut as a new version because no audit had ever graded 3.11 — its detector did not exist — so no delivered verdict moves with it. Automated checks: 43 to 42
- 2026-09-11: had `content-accessibility` report every barrier 3.11 names that markup can show, including missing alt, missing `lang` and uncaptioned video, which `image-alt-quality`, `lang-attribute` and `media-alternatives` also read — those serve 2.2, 1.14 and 1.9, and 3.11 is graded by this detector alone, so leaving them out would let the check be proposed as clear on a page a screen reader cannot use. One fact, two checks, two reasons
- 2026-09-11: failed only what WCAG level A fails outright (a link with no accessible name, an image with no alt, no page language) and made the rest a `warn` for a person: generic link text is allowed when its sentence gives the purpose (2.4.4), a table without headers may be layout, and a `<video>` without a caption track may have its captions burned into the picture. Checked against live pages: w3.org/WAI, gov.uk, bbc.com/news and kjell.com pass; arngren.net fails on 188 image links whose only image has `alt=""`
- 2026-09-11: computed a link's accessible name in the extractor, in the order the accessible-name algorithm tries (`aria-label`, `aria-labelledby`, text plus contained image alt, `title`), rather than reading `anchorText`, because an icon link with an `aria-label` is perfectly named and has no text at all — judging by text would fail every well-built social-links footer
- 2026-09-11: learned a host's HTTP version from a TLS handshake offering `h2` and `http/1.1` rather than from any fetch, because Node's `fetch` speaks HTTP/1.1 whatever the server offers — every page the crawl ever fetched was HTTP/1.1 by our choice, not the site's. The handshake is what a browser does to decide, sends no request, and is made by the crawl loop under the same delay and signal as every other visit
- 2026-09-11: read HTTP/3 from an `Alt-Svc` header on the root document rather than attempting QUIC, because that header is how a browser learns to use HTTP/3 in the first place, and Node has no stable QUIC client; an advertised `h3` passes even where the handshake chose HTTP/1.1, since the browser switches on the header
- 2026-09-11: handshook with the host the root document landed on after redirects, not the seed spelling, because the version a visitor gets is the canonical host's; and accepted any certificate for it, because a certificate problem has already failed every page fetch and the question here is only which protocol the server picks
- 2026-09-11: made HTTP/1.1 alone a `warn` rather than a `fail`, alongside missing compression and missing security headers in the same check, which are held the same way; and reported a crawl with no handshake recorded as `error`, because absent is not HTTP/1.1 — a replayed crawl, or one with auxiliary requests off, simply did not ask
- 2026-09-11: committed a self-signed localhost certificate in @seo/testkit for the handshake tests rather than generating one at test time, because Node cannot mint an X.509 certificate without openssl, and CI and a Windows shell do not agree on where that is; it is valid for `localhost` and 127.0.0.1 only and guards nothing
- 2026-09-11: split 1.12 into a crawl question and an index question — `parameter-crawl-space` asks whether the parameter URL space is bounded, `faceted-nav-control` whether each filtered page has a decision behind it — because they fail independently: every filter noindexed and ten thousand of them linked, or twelve filters linked and every one an indexable duplicate of its listing
- 2026-09-11: had `faceted-nav-control` judge that each filtered page carries a decision, not which decision, because the corpus leaves the choice of landing pages to the site; what a machine can see is a filter that is indexable with no canonical (nobody decided), one kept out of the index and still submitted in the sitemap (two decisions that disagree), and one that declares itself a landing page while carrying its listing's title (a decision that is not true)
- 2026-09-11: let neither 1.12 detector pass on filter URLs the crawl found and did not fetch, holding the check with `warn` instead — whether a parameter space closes is only observed by walking it, and a filter URL left in the frontier might lead nowhere or to a thousand more. `pagination-crawl-path` set the precedent for "linked but not reached". Found against kjell.com, where the first draft passed 23 unfetched filter URLs as bounded
- 2026-09-11: recorded URLs linked past `maxDepth` in `CrawlResult.notReached` rather than dropping them, because the depth budget is a limit of ours like the page budget and the field already promised "discovered but not fetched because a budget ran out"; stacked filters live exactly one click past wherever the crawl stops, so a crawl reporting nothing left over there was describing its own horizon as the site's edge. Kept out of `queued`, so the walk itself is unchanged
- 2026-09-11: recognised a session identifier only by names with no other meaning (`jsessionid`, `phpsessid`, `sessionid` and a few platform-specific ones), not `sid` or `session`, because those are a section or a conference session on enough sites that failing them would teach people to ignore the finding
- 2026-09-11: failed a crawl budget spent on parameter variants only when they are at least ten URLs and half of what was left unfetched, and only variants of routes the crawl already fetched; ten is a floor against a crawl that happened to end beside a sort link, and the route test keeps a crawl that ran out of *documents* from reading as one that ran out on permutations
- 2026-09-11: read campaign parameters from the raw href on internal links rather than from crawled URLs, because normalization strips them — the crawl never saw them as separate addresses, and every other crawler does; they are a `warn`, since they are as many as there are links rather than unbounded
- 2026-09-11: read "resource contention" as contention for the thing an audit borrows, not the machine it runs on — the site being crawled. The machine's side was already bounded by `concurrency`; what was not bounded was two leased workers, each with a free slot and a free lane in its own memory, crawling one site together. That became possible when leases let a second worker share a namespace (2026-09-08) and nothing had caught up with it
- 2026-09-11: laned audits on the host rather than the origin, superseding the 2026-09-04 origin lane: every crawl requests all four scheme/www spellings of its seed and follows redirects between them, so `http://example.com` and `https://www.example.com` were two lanes reaching one server. A non-default port stays in the key because a service on its own port is usually its own process, and other subdomains stay apart because the crawl never leaves its host and `blog.` is often another platform
- 2026-09-11: put the cross-worker lane check in the store (`JobStore.acquire`) rather than in a Postgres lock the queue holds for the length of a run, because a session-level advisory lock pins a pooled connection for minutes per audit and dies with the connection rather than with the lease; the jobs table already says who is running what and whether their claim is live, so the lane is a question about rows that already exist and needs no migration
- 2026-09-11: serialized `acquire` with a transaction-scoped advisory lock on `(queue, lane)`, because a check-then-write under READ COMMITTED lets two workers asking at once each read no running job and each write their own — the race test fails three runs in three without it
- 2026-09-11: let only a *live* claim hold a lane, so a worker that dies mid-crawl stops holding its site when its lease ages out, exactly when its job becomes anybody's; waiting on the row instead would make one crash stall a site until someone cleaned the table
- 2026-09-11: excluded the asking worker's own jobs from the lane check, since its in-memory lanes already serialize them and a finished job's row outlives its run by one best-effort write — counting it would stall the next audit of that site behind a delete
- 2026-09-11: made a lane refusal cost no attempt and wait one heartbeat, and made a store that cannot answer count as a refusal: running on the store's silence is the second crawler this exists to prevent, and a store unreachable for long is one whose leases are expiring anyway
- 2026-09-11: had `load` write recovered jobs back as `queued`, because a row left reading `running` by a dead process would hold its lane against every other worker for as long as the job waited for a slot — possibly the whole of a retry backoff
- 2026-09-11: kept lanes per queue namespace rather than global, because lease freshness is a property of the namespace's own `leaseMs` and one namespace per queue is already the rule
- 2026-09-11: recognised a gzip sitemap by its magic bytes rather than by `Content-Type` or a `.gz` suffix, because servers label the file `application/gzip`, `x-gzip`, `octet-stream` and occasionally `text/xml`, and a URL suffix says what someone named the file, not what the server sent; the two bytes are the only thing every gzip file agrees on
- 2026-09-11: applied the sitemap ceiling to the expanded size, as the protocol does, which also bounds a small file that inflates without end — measuring the compressed size would have let 50 MB of gzip expand to gigabytes in memory
- 2026-09-11: read a damaged gzip file as far as it decompresses and marked it truncated, rather than failing the fetch, because the server did answer and `error` means no response was obtained; detectors already report a truncated sitemap as unobservable. A connection that drops mid-file is still a failed fetch — the two are told apart by which side of the decompressor the error came from
- 2026-09-11: made `byteLength` of a truncated body a lower bound rather than the full size, since knowing the full size meant downloading it, and not downloading it is what the limit is for; nothing downstream judges a page by its size, and `truncated` says the number is partial
- 2026-09-11: stopped binary bodies at the same limit as textual ones, because the walk follows links and a link to a video file was being read whole into memory only to be measured
- 2026-09-11: raised the body limit for sitemaps alone, to sitemaps.org's 50 MB, which the 2026-09-08 decision declined to do for bodies in general: a page limit is our guess and raising it only moves the cliff, but this ceiling is the protocol's, so a sitemap read up to it has been read as far as any consumer is promised. Found by `npm run analyze` against ign.com, whose 4–7 MB quarterly video sitemaps left 2.14 and 2.1 at `probe-error`
- 2026-09-11: parsed sitemaps as a stream of SAX events rather than buffering them for cheerio, because a DOM of a 50 MB file costs several times that in memory for a result that is only a list; what the parser holds is now the entry it is inside
- 2026-09-11: dropped the entry a cut severed instead of returning it, since a severed `<loc>` is half a URL that went into the crawl frontier and a severed video entry is a field "missing" by our cut; the document is still marked truncated, because what was read is still partial
- 2026-09-08: replaced the sleeps that decided a retry had not fired with assertions on the queue’s own state — the job is out of the queue and the queue is idle, so no later moment can run it — because a bare sleep raced the queue’s own timers (a 50ms backoff against vitest’s 50ms waitFor poll) and failed once in five runs. Waiting on the retrying event rather than on state === queued also removed a second ambiguity: a job reads as queued both before its first attempt and while it waits out a backoff
- 2026-09-08: counted a `<video>` element as a video whether or not the markup says where its media comes from, after `npm run analyze` against euronews.com found eight pages with `<video class="js-native-video">` and no `src` — the source is assigned in script. Deriving "there is a video here" from a URL rather than from the element made all three detectors silent on exactly the pages that most needed them, and the site's 2.14 came back a false pass
- 2026-09-08: kept the markup-vs-player comparison on the URLs the markup does state, since a player sourced in script names no video to disagree about; it is the *count* that a sourceless player contributes, which is what "plays more videos than it describes" needs
- 2026-09-09: gave each sitemap declared in robots.txt a lane of its own and spent the fifty-document budget round-robin across them, after `npm run analyze` against ign.com read 510,000 article URLs and none of the 440 video entries sitting in a single quarterly file — `sitemap-videos.xml` is declared fourth and the article index ahead of it has more children than the budget. What a site declares first is not what an audit needs most
- 2026-09-09: accepted that the change reads fewer URLs from the largest sitemap (IGN: 510k to 181k), because a sample of every declared sitemap answers more checks than an exhaustive read of one; the budget exists to bound requests, and nothing in the corpus asks for a complete URL inventory
- 2026-09-08: made a truncated body a fact on `FetchResult` rather than raising the 5 MB limit, because raising it only moves the cliff: TED's video sitemap is 10 MB, the next site's is 40, and a body cut at any limit parses cleanly and ends in a severed entry that reads as a missing field. Found by `npm run analyze` reporting "1 of 3607 video sitemap entries unusable" against a sitemap whose 7632 entries are all complete
- 2026-09-08: had the affected detectors report `error` rather than narrowing what they judge, because the grader already means the right thing by it — the check goes ungraded with basis `probe-error` — and "we could not read this" is exactly what happened. `index-bloat` is included: a partial sitemap made it pass 2.1 on TED, which is the same bug pointing the other way
- 2026-09-08: kept a truncated sitemap's parsed URLs rather than discarding them, since the URLs read are real and useful for the crawl frontier; it is only the *completeness* claims — "absent from the sitemap", "this entry omits a field" — that the flag forbids
- 2026-09-08: split `probe fails` from `probe errors` in the analyzer's report, because printing them as one list is how a limit of the engine's gets read as a fault of the site's
- 2026-09-08: split 2.14 into a page a video sits on, the markup describing it, and the file listing it, because the three fail independently — a perfect watch page can carry no markup, complete markup can name a video that was replaced, and a video sitemap can 404 while every page on the site is fine
- 2026-09-08: judged every page carrying a video as a candidate watch page, rather than trying to find the "important" videos the corpus names, because ranking a site's own library is a person's decision; what a crawl can answer is whether this video's page is fit to be found, and that question needs no ranking
- 2026-09-08: taught the extractor about `<iframe>` and kept the list of which hosts are players in the probe, because an iframe is how nearly all web video arrives and almost none of it is video — maps, forms, adverts and consent widgets travel the same way — and naming the players is the only way to be sure the subject is a video. A player missing from the list makes the detector quiet rather than wrong
- 2026-09-08: asked robots.txt about `Googlebot-Video` and `Googlebot-Image` rather than `Googlebot`, because those are the agents that actually fetch a video file and its thumbnail; both fall back to the `*` group, which is what nearly every site writes
- 2026-09-08: compared a VideoObject's `contentUrl`/`embedUrl` to the embedded player by media id — the `v` parameter or the last path segment — because one YouTube video is spelled `watch?v=ID`, `embed/ID` and `youtu.be/ID` depending on who wrote it down, and comparing whole URLs would report every correct embed as a mismatch
- 2026-09-08: made a video sitemap's absence not-applicable rather than a finding: the corpus asks for one only "when it materially improves discovery", which is a judgement about a site's traffic, and a watch page missing from the ordinary sitemap is already `index-bloat`'s finding. A sitemap whose name says video and whose server says 404 is the case that does fail, because the site believes it is publishing one
- 2026-09-08: left the liveness of a video sitemap's `<loc>` entries to `sitemap-validity`, since they are ordinary `<url>` entries and already in `sitemapUrls`; what `video-sitemap` judges is what only it can see — whether the video declarations are complete, on this origin, and about videos the pages carry
- 2026-09-08: matched sitemap video elements on their local name rather than on the `video:` prefix, because only the namespace URI is fixed and a file binding the extension to `vid:` is equally valid — a parser keyed to the common spelling would read it as having no videos at all
- 2026-09-08: recorded each sitemap document's fetch on `CrawlResult.sitemaps`, because "declares a sitemap that 404s" and "declares no sitemap" produce the same empty URL list, and 2.14's "fetchable" is exactly the difference between them
- 2026-09-08: made `schema-eligibility-matrix` (2.7) site-scoped rather than page-scoped, because the failure the check most wants caught is a site that declares templates eligible for structured data and marks none of them up — a fact no single page can observe about itself. Per-page defects travel in `samples`
- 2026-09-08: answered only the third of 2.7 that a crawl can answer. The matrix's owner and source columns are paperwork; what the paperwork exists to protect is whether the markup names types it may name, carries what those types require, and describes something the page shows, and that is what the detector judges
- 2026-09-08: held only *subject* properties to the visible-content test — an `Article` headline, a `Product` name — and never an `Organization` or `WebSite` name, which belong to the site rather than the page and commonly appear only in a logo; testing those would have failed almost every correctly marked-up site
- 2026-09-08: left an unrecognised `@type` alone instead of reporting it as ineligible, because schema.org is vast and Google is not its only consumer: "this engine holds no requirements for Dataset" is a fact about the engine, not a finding about the site
- 2026-09-08: made FAQPage and HowTo a `warn` rather than a `fail`, following the corpus's own wording — FAQ rich results ended 7 May 2026 and HowTo before it, but accurate existing markup may stay, so the finding is about what to build next
- 2026-09-08: reported defects before absence, so a page whose only JSON-LD block fails to parse is described as having broken markup rather than none — the second is true of what a consumer sees and useless as an instruction
- 2026-09-08: split 1.15's two detectors by the freedom the corpus gives the site: `product-variant-canonical` judges the consistency of a rule the methodology leaves open, while `product-lifecycle-state` judges an out-of-stock URL against a rule the methodology states outright — "keep useful out-of-stock URLs available" — and only falls back to consistency for the discontinued case, where the corpus asks for a defined handling rather than a particular one
- 2026-09-08: read a product's lifecycle from schema.org `availability` and nothing else, because a page that 404s has no extracted markup to read and a URL shape says nothing about stock; a catalogue declaring no availability is reported as unobservable rather than as handled well
- 2026-09-08: let any purchasable offer settle a product as for sale, and any out-of-stock offer outrank a discontinued one, so a `ProductGroup` with one variant retired and another in stock is a product you can still buy — treating its URL as gone is the mistake the check exists to catch
- 2026-09-08: counted a canonical onto a *different route* as lifecycle consolidation and one onto the same route as variant consolidation, so `product-lifecycle-state` and `product-variant-canonical` cannot report the same fact twice under different names
- 2026-09-08: made noindex plus a live sitemap entry the lifecycle detector's structural finding, since `sitemap-canonical-agreement` already covers a listed URL canonicalizing away and `index-bloat` covers the reverse — a sitemap asks for indexing and noindex refuses it, and nothing else was catching a retired product issuing both
- 2026-09-08: had `product-variant-canonical` judge the *consistency* of a catalogue's canonical rule rather than the rule itself, because both usual rules are defensible — consolidate every spelling onto the product, or let each variant be its own indexable page — and only the site knows which it chose. The third state, where the template decides case by case, is the one a machine can see and the one 1.15 exists to prevent
- 2026-09-08: made a family "pages sharing a route", i.e. the same path with different query strings, rather than trying to name which parameters select a variant; whatever `?color=red` or `?sessionid=` means, the site is serving one product at two addresses, and that is the whole subject
- 2026-09-08: identified a product page only from what it declares — Product structured data or `og:type` — because guessing from URL shape would pull category listings and search results into product families, and their duplicates are other checks' business
- 2026-09-08: reported a catalogue whose products were each crawled at exactly one address as not-applicable rather than a pass, because a controlled catalogue and a crawl that never reached a variant URL are indistinguishable from here
- 2026-09-08: treated identical titles across self-canonical variants as the failure under that rule, since "each variant is its own page" is only true while the pages differ; a shared title is the uncontrolled duplicate the corpus names
- 2026-09-08: split 1.14 between `hreflang-implementation` and `hreflang-cluster-qa` by the question each answers rather than by scope — the cluster check asks whether the pages agree with each other, and the implementation check asks whether what they agree on names anything. The two come apart completely: a cluster can be flawlessly reciprocal and entirely inert because every page in it reciprocates `en-UK`, which is well-formed and names no country
- 2026-09-08: shipped the ISO 639-1 and ISO 3166-1 lists rather than a regex, because "supported language-region codes" is the corpus's wording and a regex can only answer the different question 4.9 already asks; the value of the detector is precisely that `en-UK` passes every syntax test there is
- 2026-09-08: named the replacement in the message wherever one exists — `GB` for `UK`, `he` for the withdrawn `iw` — because the tags this catches are typed from memory, and a report that says only "unsupported" sends someone to look up what they already thought they knew
- 2026-09-08: treated an x-default pointing outside the cluster as correct rather than as a broken fallback, because a language selector is its documented use; the shapes that are wrong are two x-defaults on one page, which makes the fallback ambiguous, and one beside a single locale, which leaves nothing to fall back from
- 2026-09-08: had `locale-canonical` decide whether a page is a locale variant from the cluster rather than from the page's own annotations, because the case most worth catching is the page that carries none — the cluster says it is a variant while its canonical says it is a duplicate, and a page-only reading would call that not-applicable and say nothing
- 2026-09-08: made a locale variant's canonical pointing anywhere but itself a `fail`, where the general `canonicalization` detector only warns, because a canonical elsewhere is legitimate for a known duplicate and never legitimate for a locale — it is the one instruction that outranks every hreflang annotation on the site
- 2026-09-08: added the authored `href` to `Hreflang` alongside the resolved URL, mirroring `ExtractedLink`, because a relative hreflang href is not untidiness that resolution can paper over: the annotation is discarded, and only the raw attribute shows it
- 2026-09-08: had `hreflang-implementation` fail a site whose profile says `multilingual` and whose pages carry no annotation at all, rather than reporting not-applicable as the cluster check does, because with the flag in hand silence is the finding
- 2026-09-08: made leases opt-in with `leaseMs` rather than the default, because turning them on changes what a restart means — a process that comes back under a new name has to wait out its own claim — and a store that quietly did that would have made every existing single-owner deployment slower to recover for a guarantee it was not asking for
- 2026-09-08: measured lease expiry with Postgres's `now()` rather than the process's, on the same grounds as the reconcile cutoff: every timestamp being compared was written by the database, and a claim decided by two machines agreeing about the time would eventually hand one worker's job to another over nothing but clock skew
- 2026-09-08: had a lost lease settle the job as `failed` without asking the retry policy, because a retry here would be this process running what the new owner already is — the exact thing the origin lane exists to prevent, arrived at from the other direction
- 2026-09-08: made `save` and `remove` refuse to touch a row another worker holds, and had `save` say so by throwing, because a superseded process finishing its run would otherwise overwrite the new owner's state or delete their job outright; the throw is also what lets the queue stand down at once instead of waiting for the next heartbeat to tell it the same thing
- 2026-09-08: had `runAudit` write nothing to the `audits` row when the lease went, rather than closing it out as cancelled — the run stopping is true of this process, not of the audit, and the row belongs to whoever is running it now
- 2026-09-08: taught `reconcile` to ask the store what is outstanding for *anyone* before writing an audit off, because with two workers the in-memory queue is no longer the whole answer to "is anything going to run this" and the sweep would otherwise fail exactly the audits the other worker was quietly getting on with
- 2026-09-08: left ownership recorded and unenforced on the write path, and put the guarantee in the claim instead — a worker only runs what `load` handed it, and only holds it while it keeps renewing — because enforcing it per statement would mean a lock protocol the queue would have to understand, and the interface is deliberately three methods with no locking in them
- 2026-09-08: added `sites.aiPolicy` as jsonb rather than a table or an enum, because the shape is a map from crawler name to stance and crawler names change faster than anything needing a migration should; the policy joins `flags` as the second thing on a site record that a person states and no crawl can derive
- 2026-09-08: had `ai-crawler-directive-verify` compare robots.txt against the policy in *both* directions, because the missed case is the welcoming one — a blanket disallow written years ago quietly excludes the crawler someone has since decided to court, and only the policy makes that visible
- 2026-09-08: treated a disallowed crawler still receiving a 200 as normal rather than a defect: robots.txt asks and well-behaved crawlers comply, so robots-only enforcement is the common shape. Only the reverse — a welcomed crawler turned away at the edge — is infrastructure contradicting a decision
- 2026-09-08: sent user-agent tests honestly, as real requests carrying the named crawler's user-agent, and capped them at twelve, because each is a real visit to someone's origin and a policy naming forty crawlers must not cost forty visits
- 2026-09-08: moved the AI-policy parse ahead of the `audits` insert in `submit()` after a test caught the ordering: a policy the engine cannot read is a bad request, and a bad request must leave nothing behind rather than a `pending` row waiting for the reconcile sweep to explain it
- 2026-09-07: put auxiliary requests in `crawl()` rather than letting a probe fetch for itself, because politeness is owed to a host and the crawl loop is the only thing that knows what was promised — the same delay and the same cancellation signal cover them, where a probe making its own requests would be a second, unmetered visitor to a site that agreed to one
- 2026-09-07: skipped host variants for a seed whose host cannot have them — an IP literal, `localhost`, any single-label name — because `www.127.0.0.1` is not a spelling of anything, and reporting its DNS failure would fail every audit of a staging environment for being a staging environment
- 2026-09-07: gave `FetchResult` an optional `bytes` for small non-textual responses behind a `keepBytes` flag, rather than keeping every asset, because exactly one question today needs to look inside a file (is the favicon square) and holding megabytes of images in a crawl that already holds every page would spend a real memory budget on nothing
- 2026-09-07: read icon dimensions from PNG, ICO and SVG headers directly instead of taking an image-decoding dependency — each states its size in a fixed place near the front, and any other format returns null and is reported as unmeasured rather than guessed at
- 2026-09-07: left `ai-crawler-directive-verify` (2.9) unimplemented although it looked like a one-detector win: its "Done when" asks that robots.txt, CDN behaviour and a dated user-agent test agree with *the policy*, and the policy is a document a person approved that this engine has never seen. Checking robots.txt alone and calling it agreement would put a pass on a check nobody verified
- 2026-09-07: picked the first detector batch by what a raw crawl can honestly answer rather than by how many checks it would unblock — 24 automated checks are one detector short, but most of those detectors need Search Console, Lighthouse, RDAP, a rendered DOM or extra HTTP requests the crawler does not make, and shipping a probe that guesses at those would put a `pass` on a check nobody verified
- 2026-09-07: kept `media-alternatives` to whether a caption track or text alternative exists, not whether it is accurate, because the corpus asks for both and only the first is a markup fact; 1.9 still needs its other detectors before the check clears, which is the mechanism that stops a partial answer reading as a whole one
- 2026-09-07: made `hreflang-cluster-qa` treat a cross-domain locale as unverified rather than broken — a crawl scoped to one origin cannot see the other side of the cluster, and multi-domain international setups are a normal shape, so counting them as defects would train people to ignore the detector
- 2026-09-07: had `breadcrumb-navigation` read the visible trail while `breadcrumblist-schema` reads the markup, rather than folding them into one detector, because the two come apart constantly — perfect JSON-LD beside a trail a redesign removed — and 2.17 asks for them to match
- 2026-09-07: tested these four against hand-built pages instead of extending the fixture site, because each answers a question about a shape (a reciprocal cluster, a paginated series, an ancestor that 404s) and one fixture carrying every shape at once would be a site nobody has built; the markup still goes through the real `extract`
- 2026-09-07: dropped the `priority`, `automation_tier` and `remediation_class` Postgres enums, which no column ever used — an enum with no column behind it guards nothing, because the `AssertSame` line compares a union against itself, while still charging a migration for every change to it. They also mirrored corpus vocabulary specifically, and the corpus is file-backed so that a methodology revision needs no migration; keeping the types would have quietly reintroduced the coupling the corpus was kept out of the database to avoid. The rule now stated in `enums.ts`: a vocabulary earns a Postgres type by being written to a row.
- 2026-09-07: settled the corpus source of record — the TSV under corpus/source/ bootstraps exactly one event, the first compile of a version, and corpus/v<version>/*.yaml owns it from then on; the compiler now refuses to overwrite an existing version, because the old header promised hand-editable YAML while the script silently discarded those edits and both halves were true
- 2026-09-07: made a methodology revision a new version directory rather than a re-compile of a live one, since a delivered report pins `audits.corpusVersion` and has to keep explaining itself after the methodology moves on
- 2026-09-07: made the compile version a required argument with no default, because a default is how a compile meant for 4.5 lands on 4.4 and takes a year of corpus edits with it
- 2026-09-07: split the corpus suite into a frozen `provenance.test.ts` pinned to v4.4 and a `corpus.test.ts` that discovers every version directory, because one file was being asked to prove both "we reproduced the 2026 workbook" and "this corpus is well-formed" — goals that diverge the moment search changes, and which together made adding a check mean editing six numbers about a spreadsheet
- 2026-09-07: validated site flags against the pinned corpus in `runAudit`, beside the corpus version check, because `resolveScope` reads a filled-in profile as a statement — so a misspelt flag does not go unmatched, it narrows checks to `no` with a rationale that reads deliberate, and would excuse launch gates while explaining itself confidently
- 2026-09-07: put the flag vocabulary in the corpus rather than a Postgres enum, so a version that introduces a flag needs no migration; a flag no check names today may be named by the next version
- 2026-09-07: kept the two-version fixtures outside `corpus/` and numbered them 9.0/9.1, so the version-discovery loop cannot pick up a test double and a real version directory can never be shadowed by one
- 2026-09-05: made crawl cancellation a check between requests rather than an abort of the request in flight, because abandoning a response already on the wire saves the site nothing and hands the extractor a half-read body; the guarantee worth making is "no further requests", which is the one the site can feel
- 2026-09-05: made the politeness delay interruptible, because it is the one part of a crawl deliberately measured in seconds and waiting it out would have made it the floor on how long cancelling takes — nobody is owed the pause before a request that will not be made
- 2026-09-05: closed a cancelled crawl out as `cancelled` with a null error rather than `failed` with one, so a report never sends someone looking for a fault where a person simply stopped the work; `runAudit` restates `CrawlCancelledError` as `JobCancelledError` so the queue and the retry policy see one identity for it
- 2026-09-05: had `reconcile()` mark orphaned audits `failed` rather than re-enqueue them, because the crawl budget and seeds a caller asked for live only in the job payload that was lost — resubmitting under this process's defaults would quietly run a different audit than the one requested
- 2026-09-05: took reconcile's cutoff from the database clock at recovery rather than `new Date()` in the process, because every timestamp it compares against was written by Postgres and a sweep whose correctness depends on two machines agreeing about the time will eventually be wrong on a laptop that slept
- 2026-09-05: made reconcile refuse to run before `recover()` and refuse entirely without a store, because in either case every pending audit looks abandoned and the sweep would fail the whole backlog instead of the lost rows
- 2026-09-05: backed the queue with a Postgres `jobs` table rather than Redis, superseding the 2026-09-04 expectation that durability would arrive as Redis — the audit's durable record already lives in Postgres, and a second store would be a second source of truth to reconcile after exactly the restart it exists to survive, for an operational component nothing else in the system needs yet
- 2026-09-05: put durability behind a three-method `JobStore` the queue is handed, rather than in the queue itself, so @seo/queue keeps knowing nothing about Postgres and the memory-only path stays the default — a test, and the offline analyzer, must not need a database to run a queue
- 2026-09-05: made only the enqueue write blocking, and every later transition best-effort, because a job that is not written down is work a restart drops, while a `running` update that was lost costs at most one repeated run — failing an audit over a storage blip would be the opposite of what a durable queue is for
- 2026-09-05: deleted jobs from the store the moment they settle instead of archiving them, because `audits` already records what became of an audit with its status, timings and error, and a second history is only something to keep consistent with the first
- 2026-09-05: kept the attempt count across a restart and wrote it before the handler runs, so a payload that takes the process down with it burns an attempt and eventually gives up, rather than being retried by every restart forever
- 2026-09-05: let a recovered job keep whatever is left of its retry backoff instead of running it at once, because the wait exists to give whatever fell over time to get back up, and a restart is not evidence that it has
- 2026-09-05: keyed `jobs` on `(queue, id)` rather than `id`, because a job id belongs to the queue that minted it and a single-column key would have two namespaces silently overwrite each other — the namespace has to reach the constraint, not only the `where` clause
- 2026-09-05: had `load()` claim every outstanding row in its namespace rather than only rows matching its own owner string, because a restart comes back with a new pid and an owner-matched claim would strand precisely the jobs the previous process had started; single ownership is assumed and recorded, not enforced
- 2026-09-05: split retries into mechanism in @seo/queue and policy in @seo/scheduler — the queue knows how to hold a failed job back, wake it and run it again, and consults a caller-supplied policy for whether to; which failures deserve a repeat is a fact about the work, and a queue that decided it would bury that judgement where nobody looks
- 2026-09-05: made a retry policy allowed to be async, so a caller can record the decision durably before the wait starts; the scheduler uses that to put the audit row back to `pending` before the backoff, because a row reading `failed` while another attempt is already scheduled would mislead every status endpoint built on it
- 2026-09-05: enumerated the permanent audit failures (cancellation, an unknown site, a corpus this process cannot produce, a runtime error about the program) and retried everything else, because an unrecognised blip retried costs one more crawl of a site already under audit, while an unrecognised blip written off loses the audit to a cause nobody will ever see
- 2026-09-05: kept the audit id across attempts rather than opening a new audit per retry — a retry is the same audit running again, writing a second crawl under the same row, and a caller who was handed an id at submit time must not have to discover a new one to find out how it went
- 2026-09-05: gave the prototype analyzer its own scripts rather than an apps/cli package, and had it hold the whole audit in memory with no database, because its job is to measure what the engine says today — a scruffy harness that runs anywhere is worth more than a durable one, and the JSON snapshot it leaves behind is the part that has to survive
- 2026-09-05: made each snapshot record every check verdict, not just the failures, because the question the harness answers is whether a change moved coverage, and a check that quietly stopped being graded is exactly the regression a failures-only record would hide
- 2026-09-04: gave grading its own package (@seo/grader) rather than folding it into the scheduler, because reading the corpus against evidence is a judgement with its own rules and has to be re-runnable over a stored audit without re-crawling it
- 2026-09-04: settled the grader's central rule as "a machine may fail a check, but only an `automated` check may be passed by one" — a failure is a defect a probe observed, while a pass is a clearance, and the corpus already says which checks are machine-verifiable end to end
- 2026-09-04: made an unimplemented detector, an errored probe and a detector that observed nothing all leave the check at `not-started` with `unknown` coverage, because "we did not look" is not a finding about the site and 95 of 128 detectors are unimplemented, so the honest answer is the common one
- 2026-09-04: read an empty site profile as an unmade decision and a filled-in one as a statement — conditional checks stay at `review` when a site has no flags, and are narrowed to `no` with a rationale when it has flags but none that match — because treating silence as "none of these apply" would clear conditional launch gates on the strength of a form nobody filled in
- 2026-09-04: let a check whose observations were all `not-applicable` pass with coverage `not-applicable`, because a check whose subject does not exist on the site cannot fail and holding the launch on it forever would be noise; the coverage column is what keeps "verified" and "nothing to verify" distinguishable in the report
- 2026-09-04: made recording a grade a replace rather than an upsert, so a re-grade can retract an evidence link, and made it refuse to overwrite any row whose coverage is `attested`, because a human sign-off is the one part of an audit a machine cannot reproduce
- 2026-09-04: had the scheduler resolve the pinned corpus version before the crawl rather than after it, because an audit pinned to a corpus this process cannot produce is unreportable however well the crawl goes, and finding that out afterwards spends someone else's bandwidth to learn it
- 2026-08-27: split evidence (probeResults) from verdicts (checkStates) because machines observe and humans judge, which keeps every report traceable to what produced it
- 2026-08-27: chose a memory-resident crawl loop with a streaming database sink over batch-at-end because a crawl that dies at page 400 still preserves those 400
- 2026-08-27: chose file-backed immutable YAML under corpus/v4.4 over database-held checks, using text ids not foreign keys, so an audit pinned to a corpus version stays reproducible
- 2026-08-27: kept the corpus source as TSV in corpus/source/ for provenance because the compiled YAML is authoritative but the workbook export is the origin of record
- 2026-08-27: chose to keep response bodies out of Postgres, recording only content hash and object-store key, because scaling to millions of pages requires that split
- 2026-08-28: chose a client-side pre-push hook over server-side branch protection because GitHub Free does not offer protected branches on private repos
- 2026-08-28: chose Drizzle ORM with drizzle-kit generated migrations over hand-written SQL for code-first type safety against the schema
- 2026-08-28: made the triage table in scripts/triage.ts a sign-off gate because misclassifying automation tier or remediation class breaks every downstream decision
- 2026-09-04: moved master to a server-side ruleset requiring a pull request plus the test and roadmap checks, superseding the 2026-08-28 pre-push choice because rulesets are now available on this repo and --no-verify made the client-side hook unenforceable
- 2026-09-04: set required_approving_review_count to 0 on that ruleset because GitHub forbids approving your own pull request, so any higher count would deadlock a single-maintainer repo
- 2026-09-04: signed off the v4.4 triage table on the rule that a check is only `automated` when its "Done when" closes on observation alone, which moved 2.11, 3.10, 4.2, 4.5, 4.6, 5.5, 6.3, 6.8 and 7.10 to `assisted` because each needs a person to record a decision, an owner or an exception
- 2026-09-04: read "agreed budget" and "approved baseline" wording as naming an input to a check rather than an artifact a human must produce, so those rows stayed `automated` — the tier claims what can be automated, not what the probe registry has built
- 2026-09-04: chose an in-process job queue in @seo/queue over Redis or BullMQ because the queue has no consumer outside this process yet; Redis buys durability, which is only worth its operational weight once the API-triggered scheduler can promise a queued audit will run, so it is listed as its own Phase 4 item behind the same interface
- 2026-09-04: put lane exclusion in the queue rather than in the scheduler because the crawl loop's politeness delay is measured between its own requests, so two workers on one origin would each honour it and together still double the agreed load — the guarantee only holds if something upstream refuses to run them at the same time
- 2026-09-04: left retries out of the queue and kept them as their own Phase 4 item, because which failures deserve a repeat is a policy question (a transport timeout, yes; a 403 on the first request, no) and a queue that guessed would hide it
- 2026-09-04: made job cancellation cooperative — the signal is offered, the handler decides — because nothing in Node can interrupt a running handler, and a queue that reported a job as stopped while its crawler was still fetching would be lying about the load on someone's site
- 2026-09-04: had the scheduler write the audits row in submit() and return its id before the crawl starts, because the HTTP layer this is built for has to answer in milliseconds while the audit it triggered runs for minutes — the row is the handle, and polling it is what a status endpoint will do
- 2026-09-04: used the audit id as the queue's job id so cancel() and status() take the one identifier a caller was already given, rather than making callers hold an audit id and a job id and keep them paired
- 2026-09-04: stopped the scheduler short of grading: it gathers and files evidence, leaves audits.readiness null, and an audit reading `complete` means the evidence is in, not that the launch decision is made — turning probe observations into checkStates is a distinct judgement with its own package to come
- 2026-09-04: laned audits on the site origin rather than on the site id, because politeness is owed to a host and two site records could name one origin; the lane has to key on the thing the requests actually reach
- 2026-09-17: entered releases and review runs from a YAML file (`npm run release`) rather than pulling the Phase 7 API forward, because the API needs site management, auth and a server that do not exist yet, while a file is what the workbook's owners already keep; a run already logged unchanged is skipped rather than refused, so the file can be the living log and imported again, and one logged differently is refused because the table is append-only
